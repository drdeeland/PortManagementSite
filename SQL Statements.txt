Container Page:
Create Container:
INSERT INTO Container_Location (company, dest_sid, dest_tid, source_sid, source_tid, storage_id, location)
VALUES (company, dest_sid, dest_tid, source_sid, source_tid, storage_id, location);

// Variable: companyName
Get Company Containers:
SELECT * 
FROM Container_Location CL
WHERE CL.company = companyName;

PortAdmin Page:
// Variable: shipID
Ship Overview:
SELECT *
FROM Ships S
WHERE S.ship_id = shipID;

Berth Overview:
SELECT * FROM Docked_At;

// Variable: containerID
Container Overview:
SELECT *
FROM Container_Location CL
WHERE CL.container_id = containerID;

// Variable: truckID
Truck Overview:
SELECT *
FROM Trucks T
WHERE T.truck_id = truckID;